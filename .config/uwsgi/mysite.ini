[uwsgi]
chdir = /srv/market/
module = market.wsgi:application
home = /home/ubuntu/venv/

uid = deploy
gid = deploy

http = :8080

enable-threads = true
master = true
vacuum = true
pidfile = /tmp/market.pid
logto = /var/log/uwsgi/market/@(exec://date +%%Y-%%m-%%d).log
log-reopen = true
